#pragma once

namespace config {

// Serial Configuration parameters:

extern const unsigned long
    DEFAULT_BAUD_RATE; // Default baud rate for serial communication

// LED Configuration parameters:

extern const unsigned int LED_BRIGHTNESS; // LED brightness

// WiFi Configuration parameters:

extern const char *WIFI_SSID; // WiFi SSID

extern const char *WIFI_PASSWORD; // WiFi password

extern const char *WIFI_HOSTNAME; // WiFi hostname

} // namespace config